manta {
  http {
    port: 8080
    host: "test-host"
  }
  database {
    user: ${PG_USER}
    password: "test-password"
    url: "test-url"
    driver: "org.postgresql.Driver"
    schema: "test-schema"
  }
}
