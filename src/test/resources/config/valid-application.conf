manta {
  http {
    host: "test-host"
    port: 8080
  }
  database {
    user: "test-user"
    password: "test-password"
    url: "test-url"
    driver: "org.postgresql.Driver"
    schema: "test-schema"
  }
}
