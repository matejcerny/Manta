manta {
  http {
    port: "WRONG"
    host: "test-host"
  }
  database {
    user: "test-user"
    password: "test-password"
    url: "test-url"
    driver: "org.postgresql.Driver"
    schema: "test-schema"
  }
}
