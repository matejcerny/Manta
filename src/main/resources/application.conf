manta {
  http {
    host: "localhost"
    port: 8080
  }
  database {
    user: ${PG_USER}
    password: ${PG_PASSWORD}
    url: ${PG_URL}
    driver: "org.postgresql.Driver"
    schema: "manta"
  }

}
